#  **`现有实体的名称 (Existing Entity Name)`**:
{existing_entity_name}

#  **`现有实体的摘要 (Existing Entity Summary)`**:
{existing_entity_summary}

---

#  **`待判断实体的名称 (New Entity Name)`**:
{new_entity_name}

#  **`待判断实体的摘要 (New Entity Summary)`**:
{new_entity_summary}

请仔细阅读并比较 `待判断实体的摘要 (New Entity Summary)` 与 `现有实体的摘要 (Existing Entity Summary)` 以及 `现有实体的名称 (Existing Entity Name)`。

**您的输出必须严格遵守以下规则：**

*   **如果，根据摘要信息判断，这两个实体指向的是【同一个】概念实体** (例如，`待判断实体的摘要` 只是对 `现有实体` 的补充描述、后续行动或不同侧面的描写)：
    *   **请不要输出任何字符。** 您的输出必须是一个完全空的字符串 (`""`)。

*   **如果，根据摘要信息判断，这两个实体指向的是【不同的】概念实体** (例如，它们是同名的不同人物、不同物品、不同地点，或者一个名称指代人物而另一个指代物品等)：
    *   **请为 `待判断实体的摘要` 所描述的实体想一个新的、能够区分开的名称。**
    *   **您的输出必须【仅包含】这个新的名称字符串。** 不要添加任何解释、前缀、后缀或标点符号（除非新名称本身包含）。
    *   例如，如果现有实体是“炎龙剑 (神兵利器)”，待判断实体也是“炎龙剑 (一条龙的名字)”，那么您可以输出 “炎龙 (神兽)” 或 “炎龙剑 (龙族)”。选择一个简洁且能区分的即可。

请直接输出结果，无需任何额外解释。
